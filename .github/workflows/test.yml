name: "Test"

on:
  merge_group:
  workflow_dispatch:
  workflow_call:
  pull_request:
  push:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  cargo-fmt:
    name: Cargo fmt
    runs-on: ubuntu-latest

    steps:
      - name: Fetch Repository
        uses: actions/checkout@v6

      - name: Install stable toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: rustfmt

      - name: Rustfmt Check
        run: cargo fmt --all --check

  cargo-clippy:
    name: Cargo clippy
    runs-on: ubuntu-latest

    steps:
      - name: Fetch Repository
        uses: actions/checkout@v6

      - name: Install stable toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1
        with:
          components: clippy

      - name: Clippy Check
        run: cargo clippy --locked --workspace --all-targets --all-features -- -Dwarnings

  wheel-smoke:
    name: Wheel smoke build (PR)
    if: ${{ github.event_name == 'pull_request' }}
    runs-on: ubuntu-latest

    steps:
      - name: Fetch Repository
        uses: actions/checkout@v6

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.14"

      - name: Build wheel (smoke check only)
        uses: PyO3/maturin-action@v1
        with:
          command: build
          args: --release --out dist --find-interpreter
          manylinux: off

  build-and-pytest:
    name: Build & Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [ "3.14" ]

    steps:
      - name: Fetch Repository
        uses: actions/checkout@v6

      - name: Setup mold linker
        uses: rui314/setup-mold@v1
        with:
          make-default: true

      - name: Install stable Rust toolchain
        uses: actions-rust-lang/setup-rust-toolchain@v1

      - name: Install cargo-nextest
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-nextest

      - name: Run cargo nextest in ferrobus-core
        run: |
          cd ferrobus-core
          cargo nextest run --locked --no-fail-fast

      - name: Install UV
        uses: astral-sh/setup-uv@v7
        with:
          version: "0.9.6"

      - name: Build & Test Python Extension
        run: |
          uv python install "${{ matrix.python-version }}"

          uv venv -p "${{ matrix.python-version }}" .venv
          source .venv/bin/activate

          uv sync --locked --dev

          maturin develop
          pytest
